const { expect } = require('chai');

// it('testing that we are in the same reality', () => {
//   const num1 = 2;
//   const num2 = 3;

//   expect(num1 + num2).to.equal(5);
//   expect(num1 + num2).not.to.equal(6);
// });
